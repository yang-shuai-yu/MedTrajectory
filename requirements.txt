# ---------------------------------------------------------------------------
# MedTrajectory - core runtime requirements
# Python >= 3.10
#
# Derived from the actual imports in src/ and scripts/ (not guessed).
#
# For a CPU-only PyTorch install, use requirements_cpu_torch.txt instead, which
# pins torch to the CPU wheel index; see also scripts/setup_dcs_cpu_venv.py.
# ---------------------------------------------------------------------------

# core numerical / modelling
numpy>=1.26
torch>=2.7
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0

# survival modelling (Cox baseline)
lifelines>=0.27

# plotting (figures)
matplotlib>=3.7
umap-learn>=0.5

# configuration and reporting
PyYAML>=6.0
python-docx>=1.1
Pillow>=10.0
pdf2image>=1.16
